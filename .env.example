# Generator agent (Claude) — writes the copy-paste-ready prompt.
ANTHROPIC_API_KEY=

# Architect + Reviewer agents (DeepSeek). DeepSeek is OpenAI-API-compatible
# and is reached via the openai SDK with a base_url override.
DEEPSEEK_API_KEY=
