# Generator agent (writes the visual prompt).
ANTHROPIC_API_KEY=

# Reviewer agent (scores the prompt). DeepSeek is OpenAI-API-compatible and
# is reached via the openai SDK with a base_url override.
DEEPSEEK_API_KEY=
